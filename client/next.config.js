/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['img.freepik.com', 'example.com','images.unsplash.com','t4.ftcdn.net']
      },
}

module.exports = nextConfig
